package com.ajsg2.minimaljava.common.tokens.simple;

import com.ajsg2.minimaljava.common.tokens.SimpleToken;

public class Period extends SimpleToken {

    public Period(int line, int col) {
        super(line, col);
    }
}
